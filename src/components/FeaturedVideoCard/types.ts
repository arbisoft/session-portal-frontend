export type FeaturedVideoCardProps = {
  className?: string;
  description: string;
  event_time: string;
  onClick?: VoidFunction;
  organizer: string;
  thumbnail: string;
  title: string;
  video_duration: string;
  width?: string;
};
