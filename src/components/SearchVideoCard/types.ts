export type SearchVideoCardProps = {
  className?: string;
  description: string;
  event_time: string;
  organizer?: string;
  thumbnail: string;
  title: string;
  video_duration: string;
  width?: string;
  onClick?: VoidFunction;
};
